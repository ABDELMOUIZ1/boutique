spring:
  application:
    name: notification-service
  cloud:
    config:
      uri: http://localhost:8071 # Adresse du Config Server
      fail-fast: true
  config.import:
    configserver: # Import des configurations depuis le Config Server

